{
  "name": "redis-multi-desktop",
  "version": "1.0.0",
  "description": "",
  "main": "app/main/index.js",
  "dependencies": {
    "electron": "^20.0.2"
  },
  "config": {
    "renderdir": "app/renderer/redis-multi-desktop"
  },
  "scripts": {
    "prestart": "cd $npm_package_config_renderdir && npm run dev &",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": " sleep 3 && electron ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
